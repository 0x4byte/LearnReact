import React from 'react';
// import CounterContainer from './containers/CounterContainer';
import CounterOptimizeContainer from './containers/CounterOptimizeContainer';

export default () => {
  return (
    <div>
      {/* <CounterContainer /> */}
      {<CounterOptimizeContainer />}
    </div>
  );
};
